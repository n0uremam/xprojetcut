[build]
  functions = "netlify/functions"
  # Serve the static directory directly when it is the publish target.
  publish = "."
[functions]
  included_files = ["app.py", "templates/**", "static/**"]
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app/:splat"
  status = 200
