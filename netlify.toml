[build]
  functions = "netlify/functions"
  # Serve the built static directory while letting redirects route requests to the Function.
  publish = "static"
[functions]
  included_files = ["app.py", "templates/**", "static/**"]
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app/:splat"
  status = 200
